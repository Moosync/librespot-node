pub struct PlayerConstructorConfig {
    pub username: String,
    pub password: String,
    pub auth_type: String,
    pub backend: String,
    pub normalization: bool,
    pub normalization_pregain: f64,
}
